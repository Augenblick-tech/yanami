[package]
name = "user"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1.81"
domain = { path = "../domain" }

[dev-dependencies]
tokio = { version = "1.39.2", features = ["macros", "rt-multi-thread"] }
